development:
  driver: postgres
  open: 'postgres://postgres:@docker/tracing_rest_dev?sslmode=enable'

test:
  driver: postgres
  open: dbname=tracing_rest_test sslmode=disable

ci:
  driver: postgres
  open: dbname=circle_test sslmode=disable

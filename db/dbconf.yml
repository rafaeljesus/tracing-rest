development:
  driver: postgres
  open: dbname=tracing_rest_dev sslmode=disable

test:
  driver: postgres
  open: dbname=tracing_rest_test sslmode=disable

ci:
  driver: postgres
  open: dbname=circle_test sslmode=disable
